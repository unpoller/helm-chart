apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "unpoller.fullname" . }}
  labels:
    {{- include "unpoller.labels" . | nindent 4 }}
data:
  up.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
